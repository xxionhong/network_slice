#After installed mininet

sudo ovs-vsctl -V

#remove old ovs
sudo apt-get remove openvswitch-common openvswitch-switch openvswitch-pki openvswitch-testcontroller
sed -i -e 's/for pkg in/$pkginst libopenvswitch_$OVS_RELEASE*.deb\n    for pkg in/g' ~/mininet/util/install.sh
cd ~
sudo ./mininet/util/install.sh -n3V 2.10.5

sudo ovs-vsctl -V

